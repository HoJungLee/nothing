// https://app.codility.com/demo/results/trainingQDGS9S-TD6/
int solution(int X, int Y, int D) {
    // write your code in C++14 (g++ 6.2.0)
   int temp = Y-X;
    int answer = temp / D;
    if( temp%D !=0 ){
        answer++;
    }
    
    return answer;
}
